services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: study_api
    ports:
      - "8010:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=1
      - ALLOWED_HOSTS=*
